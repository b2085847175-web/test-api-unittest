dev:
  api_base_url: "${AI_BASE_URL_DEV:https://dev.zhiyan.chat}"
  login_account: "${LOGIN_ACCOUNT_DEV:}"
  login_password: "${LOGIN_PASSWORD_DEV:}"
  chat_account: "${CHAT_ACCOUNT_DEV:娴嬭瘯涓撶敤1}"

console:
  api_base_url: "${AI_BASE_URL_CONSOLE:https://console.zhiyan.chat}"
  login_account: "${LOGIN_ACCOUNT_CONSOLE:}"
  login_password: "${LOGIN_PASSWORD_CONSOLE:}"
  chat_account: "${CHAT_ACCOUNT_CONSOLE:娴嬭瘯涓撶敤1}"
  access_token: "${ACCESS_TOKEN_CONSOLE:}"

common:
  headers:
    Content-Type: "application/json"
    User-Agent: "AI-Chat-Test"
  timeout: 30
  retry_count: 3
  retry_interval: 1
  chat_defaults:
    platform: "${CHAT_PLATFORM:tmall}"
    is_test: true
